# Runtime dependencies for the PillSync backend.
# Pinned exactly so every intern's branch, CI and production install the same
# tree. Bump a pin deliberately, in its own commit, and run the tests.

# --- Web framework ---
Django==6.0.8
djangorestframework==3.18.0
django-cors-headers==4.9.0
django-filter==26.1
drf-spectacular==0.30.0

# --- Authentication ---
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0
google-auth==2.57.1

# --- Database ---
psycopg[binary]==3.3.5
dj-database-url==3.1.2

# --- Background jobs (wired in Milestone 1, used from Milestone 2) ---
celery==5.6.3
redis==8.1.0
django-celery-beat==2.9.0

# --- Files and images ---
pillow==12.3.0

# --- Serving ---
whitenoise==6.12.0
gunicorn==26.2.0

# --- Configuration ---
python-dotenv==1.2.3

# --- Added in Milestone 3 (OCR and AI) ---
# pytesseract==0.3.13
# opencv-python-headless==4.11.0.86
# spacy==3.8.4
# openai==1.99.1

# --- Added in Milestone 2 (notifications) ---
# firebase-admin==6.6.0
# twilio==9.4.1
# sendgrid==6.11.0
