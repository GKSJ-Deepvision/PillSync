# Runtime dependencies for the PillSync backend.
# Pin a version when you add a package so every branch installs the same thing.
#
# Web framework — FastAPI (this backend's chosen stack; DRF was the spec's
# alternative but FastAPI needed nothing beyond what Module 6 requires).
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
python-dotenv==1.*

# Module 6 — AI Refill Prediction Engine (backend/apps/refills)
scikit-learn==1.5.*
joblib==1.4.*
# pandas/numpy are only needed for training (see ml/requirements.txt), not
# for serving predictions with an already-trained model.
#
# Auth
# djangorestframework-simplejwt==5.*
# django-oauth-toolkit==3.*
#
# Database
# psycopg[binary]==3.*
#
# Background jobs / scheduling
# celery==5.*
# redis==5.*
# django-celery-beat==2.*
#
# OCR and NLP
pytesseract==0.3.*
Pillow==11.*
python-multipart==0.0.*
# opencv-python-headless==4.*
# spacy==3.*
# openai==1.*
#
# Notifications
# firebase-admin==6.*
# twilio==9.*
# sendgrid==6.*

