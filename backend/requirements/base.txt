# Runtime dependencies for the PillSync backend.
# This branch uses the FastAPI alternative (see backend/README.md) — versions
# pinned so every environment installs the same thing.
#
# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings & validation
pydantic==2.10.3
pydantic-settings==2.7.0

# Auth (issuing/verifying JWTs — full auth flow lands with the accounts app)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20

# Background jobs / scheduling (used from Milestone 2 onward)
# celery==5.4.*
# redis==5.2.*
# django-celery-beat==2.*

# OCR and NLP (used from Milestone 3 onward)
# pytesseract==0.3.*
# Pillow==11.*
# opencv-python-headless==4.*
# spacy==3.*
# openai==1.*

# Notifications (used from Milestone 2 onward)
# firebase-admin==6.*
# twilio==9.*
# sendgrid==6.*

# Utilities
python-dotenv==1.0.1
